specVersion: "v1beta"
serviceId: "byl-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "byl-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
